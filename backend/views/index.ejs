<!DOCTYPE html>
<html>

<head>
    <title>
        <%= title%>
    </title>
    <link rel="stylesheet" href="/stylesheets/style.css"></link>
</head>

<body>
    <h1 class="text-center mb-4">
        <%=title%>
    </h1>
    <% spel.forEach(spels => {  %>
        <nav class="p-4  m-4 border border-dark ">
            <a href="/games/<%= spels.url_slug %>">
                <div class="row ">
                    <div class="col-9 col-sm-9 col-md-9 col-lg-9 col-xl-10 font-weight-bold">
                        <h2>
                            <%=spels.game_title%>
                        </h2>
                    </div>
    
                    <div class="col-1 col-sm-3 col-md-2 col-lg-1 col-xl-1">
                        <h3>  <%=spels.score%>
                        </h3>
                    </div>
                </div>
                <div class=" row ">
                    <div class="col-5 col-sm-4 col-md-4 col-lg-4 col-xl-2 ">
                        <div>
                            <%=spels.first_name%>
                                <%=spels.last_name%>,
                                    <% const scoreDate = new Date (spels.date); %>
        
                                        <%=scoreDate.toLocaleDateString('se-SE'); %>
        
                        </div>
                    </div>
                </div>
            </a>
        </nav>
            <% }) %>








</body>

</html>